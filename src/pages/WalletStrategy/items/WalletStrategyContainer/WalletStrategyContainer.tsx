import React, { FC } from 'react';
import WalletStrategyView from '../WalletStrategyView';

const WalletStrategyContainer: FC = () => {
  return <WalletStrategyView />;
};

export default WalletStrategyContainer;
