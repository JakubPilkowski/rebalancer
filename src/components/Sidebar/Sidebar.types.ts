type SidebarProps = {
  className?: string;
  loading?: boolean;
  hasWallets?: boolean;
  /**
   * used for mobile sidebar view to open/hide sidebar
   */
  //   isOpen: boolean;
  /**
   * used for desktop view to expand/collapse sidebar
   */
  //   isExpanded: boolean;
};

export default SidebarProps;
